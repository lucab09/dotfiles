#!/usr/bin/env sh

# Layout di default: tiling (bsp = binary space partitioning)
yabai -m config layout bsp

# Nuove finestre appaiono a destra/sotto
yabai -m config window_placement second_child

# La barra Swift è una colonna verticale larga 64 px sul bordo destro:
# `external_bar` riserva solo spazio sopra/sotto, quindi niente riserva lì
# e la colonna viene tenuta libera dal padding destro. Il solo `off` viene
# rifiutato in silenzio: serve la forma completa `off:0:0`.
yabai -m config external_bar off:0:0

# Margine esterno di 12 px su ogni lato. A destra: colonna (64) meno il
# margine interno delle card (6, card da 52 centrate) più il gap (12), così
# tra finestra e card resta lo stesso spazio degli altri bordi. Da tenere
# allineato a `Metrics.sidebarWidth` e `Metrics.cardWidth`.
yabai -m config top_padding    12
yabai -m config bottom_padding 12
yabai -m config left_padding   12
yabai -m config right_padding  70
yabai -m config window_gap     12

# Bilanciamento automatico spazio quando si apre/chiude una finestra
yabai -m config auto_balance off

# Mouse: la finestra sotto il cursore diventa attiva
yabai -m config mouse_follows_focus          off
yabai -m config focus_follows_mouse          off

# App da escludere dal tiling (lasciale floating)
yabai -m rule --add app="^System Preferences$"   manage=off
yabai -m rule --add app="^System Settings$"      manage=off
yabai -m rule --add app="^Archive Utility$"      manage=off
yabai -m rule --add app="^Finder$"               manage=off
yabai -m rule --add app="^Alfred$"               manage=off
yabai -m rule --add app="^Raycast$"              manage=off
yabai -m rule --add app="^Calculator$"           manage=off

echo "yabai configuration loaded"
